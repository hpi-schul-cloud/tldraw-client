apiVersion: v1
kind: Service
metadata:
  name: tldraw-svc
  namespace: {{ NAMESPACE }}
  labels:
    app: tldraw-client
spec:
  type: ClusterIP
  ports:
    - port: 3046
      targetPort: 3046
      protocol: TCP
      name: tldraw-client
  selector:
    app: tldraw-client