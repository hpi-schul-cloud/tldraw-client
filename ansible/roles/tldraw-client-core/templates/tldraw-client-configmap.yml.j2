apiVersion: v1
kind: ConfigMap
metadata:
  name: tldraw-client-configmap
  namespace: {{ NAMESPACE }}
  labels:
    app: tldraw-client
data:
    NODE_ENV: "production"
    TZ: "Europe/Berlin"
    PORT: "3046"
    SERVER_TLDRAW_2_ENABLED: "{{ WITH_TLDRAW2 }}"
