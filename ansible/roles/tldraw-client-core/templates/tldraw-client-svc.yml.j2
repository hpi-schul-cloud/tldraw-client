apiVersion: v1
kind: Service
metadata:
  name: tldraw-client-svc
  namespace: {{ NAMESPACE }}
  labels:
    app: tldraw-client
spec:
  ports:
    - port: 3046
      targetPort: 3046
      protocol: TCP
      name: tldraw-client
  selector:
    app: tldraw-client
  sessionAffinity: ClientIP